# Web framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0

# Anthropic SDK — unpinned upper bound since adaptive thinking / output_config.effort
# require a reasonably recent release. `pip install -U anthropic` if tool calls 400.
anthropic>=0.40.0

# HTTP client (used by the web_search tool)
httpx>=0.27.0,<1.0.0

# Data validation / settings
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# Config loading
python-dotenv>=1.0.1,<2.0.0

# Retry logic for the search tool
tenacity>=9.0.0,<10.0.0
